---
title: "word2vec 논문 서베이 - 22th"
date: 2020-11-09 00:00:00 +0000
categories: ["papers", "NLP"]
tags: ["단어임베딩", "NLTK", "Word2Vec", "gensim"]
---

## 영어/한국어 Word2Vec 실습

- 딥 러닝을 이용한 자연어 처리 입문
- 저자:
- 년도:
- https://wikidocs.net/50739
- 출처 : DB Pia (국내)

### 요약

**영어 단어임베딩**

"Here are two reasons companies fail: they only do more of the same, or they only do what's new.\nTo me the real, real solution to quality growth is figuring out the balance between two activities: exploration and exploitation. Both are necessary, but it can be too much of a good thing.\nConsider Facit"

#### 1. NLTK를 이용하여 문장 토큰화를 수행

  - 각 문장에 대해서 구두점을 제거하고, 대문자를 소문자로 변환
  - 각 문장에 대해서 NLTK를 이용하여 단어 토큰화를 수행
  - 총 샘플의 개수 : 273424
  - 예) 상위 3개 출력

```
['here', 'are', 'two', 'reasons', 'companies', 'fail', 'they', 'only', 'do', 'more', 'of', 'the', 'same', 'or', 'they', 'only', 'do', 'what', 's', 'new']
['to', 'me', 'the', 'real', 'real', 'solution', 'to', 'quality', 'growth', 'is', 'figuring', 'out', 'the', 'balance', 'between', 'two', 'activities', 'exploration', 'and', 'exploitation']
['both', 'are', 'necessary', 'but', 'it', 'can', 'be', 'too', 'much', 'of', 'a', 'good', 'thing']
```

#### 2. word2vec

```python
from gensim.models import Word2Vec
model = Word2Vec(sentences=result, size=100, window=5, min_count=5, workers=4, sg=0)
```

<br/>
여기서 Word2Vec의 하이퍼파라미터값은 다음과 같습니다.

- size = 워드 벡터의 특징 값. 즉, 임베딩 된 벡터의 차원.
- window = 컨텍스트 윈도우 크기
- min_count = 단어 최소 빈도 수 제한 (빈도가 적은 단어들은 학습하지 않는다.)
- workers = 학습을 위한 프로세스 수
- sg = 0은 CBOW, 1은 Skip-gram.

#### 3. 학습 테스트 : man과 가장 유사한 단어들은 어떤 단어들일까요?

```python
model_result = model.wv.most_similar("man")
print(model_result)

[('woman', 0.842622697353363), ('guy', 0.8178728818893433), ('boy', 0.7774451375007629), ('lady', 0.7767927646636963), ('girl', 0.7583760023117065), ('gentleman', 0.7437191009521484), ('soldier', 0.7413754463195801), ('poet', 0.7060446739196777), ('kid', 0.6925194263458252), ('friend', 0.6572611331939697)]
```

#### 4. Word2Vec 모델 저장하고 로드하기

```python
from gensim.models import KeyedVectors
model.wv.save_word2vec_format('eng_w2v') # 모델 저장
loaded_model = KeyedVectors.load_word2vec_format("eng_w2v") # 모델 로드
```

<br/>
**한국어 단어임베딩**

```text
어릴때 보고 지금 봐도 재밋어요
디자인을 배우는 학생으로서, 전통을 통해 발전해 나가는 ...
폴리스 스토리 시리즈는 1부터 뉴까지 버릴께...
와,, 연기가 진짜 개쩔구나
```

1. 불용어 정의
2. 형태소 분석기 OKT 토큰화
  - 리뷰 길이 분포 확인: 최대 72, 평균 길이 10.7
3. 워드 임베딩

```python
from gensim.models import Word2Vec
model = Word2Vec(sentences = tokenized_data, size = 100, window = 5, min_count = 5, workers = 4, sg = 0)
```

한국어의 미리 학습된 Word2Vec 모델은 박규병님의 깃허브 주소인 [https://github.com/Kyubyong/wordvectors](https://github.com/Kyubyong/wordvectors) 에 공개되어져 있습니다.

모델 다운로드 경로 :
[https://drive.google.com/file/d/0B0ZXk88koS2KbDhXdWg1Q2RydlU/view](https://drive.google.com/file/d/0B0ZXk88koS2KbDhXdWg1Q2RydlU/view)

* 참고 : [word2vec 그리고 추천시스템의 item2vec](https://brunch.co.kr/@goodvc78/16?fbclid=IwAR1QZZAeZe_tNWxnxVCRwl8PIouBPAaqSIJ1lBxJ-EKtfDfmLehi1MUV_Lk)


### 의견



### 초록



### 내용



### 결론

